# OTP Standalone App — ⏸ PAUSED (Desktop)

Part of the Shuriken Desktop App initiative (see `desktop/`).

This is a standalone Python GUI for OTP retrieval. It's on hold while
we focus on the Shuriken website (`site/quick-bites/otp.html`).

## Files

- `otp_app.py` — Standalone OTP retriever (Tkinter GUI)
- `shuriken_app.py` — Shuriken launcher variant
- `build-exe.bat` — Package as .exe via PyInstaller
