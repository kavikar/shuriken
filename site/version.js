// Single source of truth for the portal's displayed version. This is a
// plain static site with no build step, so there's no package.json to read
// this from — bump it by hand when the portal has a meaningful release.
const SITE_VERSION = '1.0.0';
